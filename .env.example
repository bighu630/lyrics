# Lyrics 应用环境变量配置文件
# 这个文件可以放在项目根目录、用户主目录 (~/.lyrics.env) 或作为 .env.local

# Gemini AI API 密钥 (必需)
GEMINI_API_KEY=your_gemini_api_key_here

# 网易云音乐 Cookie (可选，用于避免限流)
NETEASE_COOKIE=""

# 自定义缓存目录 (可选)
# LYRICS_CACHE_DIR=/custom/cache/path

# IPC Socket 路径 (可选)
# SOCKET_PATH=/tmp/custom_lyrics_app.sock

# 日志级别 (可选)
# LOG_LEVEL=debug

# Gemini 模型名称 (可选)
# GEMINI_MODEL=gemini-2.5-flash
